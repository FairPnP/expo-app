import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const StripeReturnScreen = () => {
  return (
    <View>
      <Text>StripeReturnScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
